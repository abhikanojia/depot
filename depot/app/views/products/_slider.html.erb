<ul id="slideshow" data-slider="slideshow">
  <% if @product.images.present? %>
    <% @product.images.each do |image| %>
      <li><%= image_tag(image.filename) %></li>
    <% end %>
  <% end %>
</ul>
